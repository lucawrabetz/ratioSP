#include <iostream>
#include <time.h>
#include <vector>

using std::vector;

vector<int> min_ratio_st(){

}

int main(int argc, char* argv[]) {
    const clock_t time_0 = clock();

    const std::string datafile = argv[1];
    const std::string outputfile = argv[2];

    float run_time = float(clock() - time_0) / CLOCKS_PER_SEC;

    return 0;
}
